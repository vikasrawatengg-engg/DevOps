name: Vikas
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: create a directory
        run: mkdir Vikas
       

      - name: create a file
        run: cd Vikas && touch hello.txt
        
         