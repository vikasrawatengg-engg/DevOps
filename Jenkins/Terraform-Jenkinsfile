pipeline {
    agent { 
        node { 
            label 'Jenkins-Node-01' 
        } 
    }

    environment {
        AWS_DEFAULT_REGION = 'ap-south-1' 
        // ⚠️ Verify that this matches the directory folder name containing your .tf files
        TF_DIR = 'Terraform' 
    }

    stages {
        stage('Git Checkout') {
            steps {
                echo 'Pulling infrastructure configuration blueprints from SCM...'
                git url: 'https://github.com/vikasrawatengg-engg/DevOps.git', branch: 'main'
            }
        }

        stage('Terraform Installation') {
            steps {
                echo 'Verifying and installing Terraform tool binaries...'
                sh '''
                    if ! command -v terraform &> /dev/null; then
                        echo "Terraform missing. Installing packages..."
                        sudo dnf install -y yum-utils
                        sudo yum-config-manager --add-repo https://rpm.releases.hashicorp.com/AmazonLinux/hashicorp.repo
                        sudo dnf install -y terraform
                    else
                        echo "Terraform is already available:"
                        terraform version
                    fi
                '''
            }
        }

        stage('Terraform Init') {
            steps {
                dir("${TF_DIR}") {
                    sh 'terraform init'
                }
            }
        }

        stage('Terraform Plan') {
            steps {
                dir("${TF_DIR}") {
                    withCredentials([
                        string(credentialsId: 'AWS_ACCESS_KEY_ID', variable: 'AWS_ACCESS_KEY_ID'),
                        string(credentialsId: 'AWS_SECRET_ACCESS_KEY', variable: 'AWS_SECRET_ACCESS_KEY')
                    ]) {
                        sh 'terraform plan -out=tfplan'
                    }
                }
            }
        }

        stage('Gate: Manual Gate Approval') {
            steps {
                // Blocks here cleanly waiting for your manual confirmation button click
                input message: '⚠️ Review the generated Plan matrix. Do you approve deployment?', 
                      ok: 'Approve & Apply Changes'
            }
        }

        stage('Terraform Apply') {
            steps {
                dir("${TF_DIR}") {
                    withCredentials([
                        string(credentialsId: 'AWS_ACCESS_KEY_ID', variable: 'AWS_ACCESS_KEY_ID'),
                        string(credentialsId: 'AWS_SECRET_ACCESS_KEY', variable: 'AWS_SECRET_ACCESS_KEY')
                    ]) {
                        sh 'terraform apply -input=false tfplan'
                    }
                }
            }
        }
    }

    post {
        always {
            echo 'Cleaning up cached plan fragments...'
            dir("${TF_DIR}") {
                sh 'rm -f tfplan'
            }
        }
    }
}